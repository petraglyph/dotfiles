#/bin/sh
# //

vol="$(amixer sget Master | grep 'Right:' | awk -F'[][]' '{ print $2 }')"

if [[ $vol = "0%" ]]; then
    echo ""
elif [[ ${vol:1:2} = "%" || ${vol:0:1} < 5 ]]; then
    echo ""
else
    echo ""
fi
echo ""
echo "#ffcc00"
