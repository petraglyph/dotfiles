conky.config = {
	background = false,
	out_to_x = false,
	out_to_console = true,
	update_interval = 1,
	total_run_times = 1,
	use_spacer = "none",
}

conky.text = [[
${if_existing /proc/net/route wlp59s0}${wireless_essid wlp59s0} ${wireless_link_qual_perc wlp59s0}% (${downspeedf wlp59s0}K/${upspeedf wlp59s0}K)${else}Disconnected$endif  
]]
