conky.config = {
	background = false,
	out_to_x = false,
	out_to_console = true,
	update_interval = 1,
	total_run_times = 1,
	use_spacer = "none",
}

conky.text = [[

X
\#${if_match $cpu>70}cc0000 ${else}ffcc00 ${endif}
]]
